import{_ as e,o as d,c as o,a9 as i}from"./chunks/framework.Bxc0mSCw.js";const p=JSON.parse('{"title":"ImageDiff","description":"Two pictures or a list of them, side by side, faded, wiped or masked. Every option, its type and its default.","frontmatter":{"title":"ImageDiff","order":2,"description":"Two pictures or a list of them, side by side, faded, wiped or masked. Every option, its type and its default."},"headers":[{"level":2,"title":"The pictures","slug":"the-pictures","link":"#the-pictures","children":[]},{"level":2,"title":"The view","slug":"the-view","link":"#the-view","children":[]},{"level":2,"title":"Moving around","slug":"moving-around","link":"#moving-around","children":[]}],"relativePath":"api/components/image-diff.md","filePath":"en/api/components/image-diff.md","lastUpdated":1789182987000}'),a={name:"api/components/image-diff.md"};function r(c,t,s,h,n,l){return d(),o("div",null,[...t[0]||(t[0]=[i('<h1 id="imagediff" tabindex="-1"><code>ImageDiff</code> <a class="header-anchor" href="#imagediff" aria-label="Permalink to &quot;`ImageDiff`&quot;">​</a></h1><div class="diffine-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ImageDiff</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> before</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{saved} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">after</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{rendered} /&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ImageDiff</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;editor&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> view</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;wipe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div></div><div class="diffine-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ImageDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(before</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DiffineEncodedImage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(saved), after</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DiffineEncodedImage</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rendered));</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ImageDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DiffineMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.editor, view</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DiffineImageView</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.wipe, onChoose</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> pick);</span></span></code></pre></div></div><h2 id="the-pictures" tabindex="-1">The pictures <a class="header-anchor" href="#the-pictures" aria-label="Permalink to &quot;The pictures&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>mode</code></td><td><code>&#39;viewer&#39; | &#39;editor&#39;</code></td><td><code>&#39;viewer&#39;</code></td><td>Whether the pictures are only looked at, or chosen as well.</td></tr><tr><td><code>before</code></td><td><code>DiffineImageInput</code></td><td>—</td><td>The picture on the left.</td></tr><tr><td><code>after</code></td><td><code>DiffineImageInput</code></td><td>—</td><td>The picture on the right.</td></tr><tr><td><code>pictures</code></td><td><code>readonly DiffineImageInput[]</code></td><td>—</td><td>Several pictures rather than two. Turns the list on.</td></tr><tr><td><code>baseline</code></td><td><code>number</code></td><td><code>0</code></td><td>Which of them the rest are counted against.</td></tr><tr><td><code>picturesResult</code></td><td><a href="./../types/diff-images-result"><code>DiffImagesResult</code></a></td><td>—</td><td>A comparison of the list already worked out.</td></tr><tr><td><code>onPicturesDiff</code></td><td><code>(result: DiffImagesResult | null) =&gt; void</code></td><td>—</td><td>The comparison of the list.</td></tr><tr><td><code>defaultBefore</code></td><td><code>DiffineImageInput</code></td><td>—</td><td>What the left side starts with. Editor only.</td></tr><tr><td><code>defaultAfter</code></td><td><code>DiffineImageInput</code></td><td>—</td><td>What the right side starts with. Editor only.</td></tr><tr><td><code>onBeforeChange</code></td><td><code>(value: File) =&gt; void</code></td><td>—</td><td>A picture was chosen for the left side.</td></tr><tr><td><code>onAfterChange</code></td><td><code>(value: File) =&gt; void</code></td><td>—</td><td>A picture was chosen for the right side.</td></tr><tr><td><code>onDiff</code></td><td><code>(result: DiffImageResult | null) =&gt; void</code></td><td>—</td><td>The comparison, every time it is worked out again.</td></tr><tr><td><code>result</code></td><td><a href="./../types/diff-image-result"><code>DiffImageResult</code></a></td><td>—</td><td>A comparison already worked out. The pictures are still drawn.</td></tr><tr><td><code>diff</code></td><td><a href="./../types/diff-image-options"><code>DiffImageOptions</code></a></td><td>—</td><td>How the two are compared. See <a href="./../methods/diff-image"><code>diffImage</code></a>.</td></tr><tr><td><code>maxPixels</code></td><td><code>number</code></td><td><code>4000000</code></td><td>How many pixels a picture is decoded at, at most.</td></tr></tbody></table><p><code>DiffineImageInput</code> is a picture or a picture with a name on it: <code>Blob | ImageBitmap | DiffPixels</code>, or <code>{ content, label }</code> around one of those. A URL is not among them — fetching one is the application&#39;s to do, and what arrives here is what it already holds.</p><p><code>editor</code> mode is the usual React pair. <code>defaultBefore</code> and <code>defaultAfter</code> leave the pictures to the component; <code>before</code> and <code>after</code> make them the application&#39;s, and <code>onBeforeChange</code> and <code>onAfterChange</code> are called either way.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>mode</code></td><td><code>DiffineMode</code></td><td><code>DiffineMode.viewer</code></td><td>Whether the pictures are only looked at, or chosen as well.</td></tr><tr><td><code>before</code></td><td><code>DiffineImageContent?</code></td><td>—</td><td>The picture on the left.</td></tr><tr><td><code>after</code></td><td><code>DiffineImageContent?</code></td><td>—</td><td>The picture on the right.</td></tr><tr><td><code>beforeLabel</code></td><td><code>String?</code></td><td>—</td><td>What the header calls the left side.</td></tr><tr><td><code>afterLabel</code></td><td><code>String?</code></td><td>—</td><td>What it calls the right side.</td></tr><tr><td><code>pictures</code></td><td><code>List&lt;DiffineImageContent&gt;?</code></td><td>—</td><td>Several pictures rather than two. Turns the list on.</td></tr><tr><td><code>pictureLabels</code></td><td><code>List&lt;String&gt;?</code></td><td>—</td><td>What each of them is called.</td></tr><tr><td><code>baseline</code></td><td><code>int</code></td><td><code>0</code></td><td>Which of them the rest are counted against.</td></tr><tr><td><code>picturesResult</code></td><td><a href="./../types/diff-images-result"><code>DiffImagesResult?</code></a></td><td>—</td><td>A comparison of the list already worked out.</td></tr><tr><td><code>onPicturesDiff</code></td><td><code>ValueChanged&lt;DiffImagesResult?&gt;?</code></td><td>—</td><td>The comparison of the list.</td></tr><tr><td><code>onChoose</code></td><td><code>Future&lt;DiffineImageContent?&gt; Function(DiffineSide)?</code></td><td>—</td><td>A reader asked for a picture. Editor only.</td></tr><tr><td><code>onDiff</code></td><td><code>ValueChanged&lt;DiffImageResult?&gt;?</code></td><td>—</td><td>The comparison, every time it is worked out again.</td></tr><tr><td><code>result</code></td><td><a href="./../types/diff-image-result"><code>DiffImageResult?</code></a></td><td>—</td><td>A comparison already worked out. The pictures are still drawn.</td></tr><tr><td><code>diff</code></td><td><a href="./../types/diff-image-options"><code>DiffImageOptions</code></a></td><td><code>kDiffineImageDefaults</code></td><td>How the two are compared. See <a href="./../methods/diff-image"><code>diffImage</code></a>.</td></tr><tr><td><code>maxPixels</code></td><td><code>int</code></td><td><code>4000000</code></td><td>How many pixels a picture is decoded at, at most.</td></tr></tbody></table><p><code>DiffineImageContent</code> is a sealed class with three shapes: <code>DiffineEncodedImage</code> around the bytes of a file, <code>DiffineDecodedImage</code> around a <code>ui.Image</code> the application already has, and <code>DiffinePixelImage</code> around a <code>DiffPixels</code>. A URL is not among them — fetching one is the application&#39;s to do, and what arrives here is what it already holds.</p><p><code>onChoose</code> is the editor&#39;s, and it is the whole of it: the widget asks for a picture for one side and the application answers with one, or with <code>null</code> for a reader who changed their mind. A file picker is a plugin and a permission, and neither belongs inside a diff viewer.</p></div><h2 id="the-view" tabindex="-1">The view <a class="header-anchor" href="#the-view" aria-label="Permalink to &quot;The view&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>view</code></td><td><code>&#39;split&#39; | &#39;overlay&#39; | &#39;wipe&#39; | &#39;mask&#39;</code></td><td><code>&#39;split&#39;</code></td><td>How the two are laid out.</td></tr><tr><td><code>unchanged</code></td><td><code>&#39;keep&#39; | &#39;dim&#39; | &#39;hide&#39;</code></td><td><code>&#39;keep&#39;</code></td><td>What is done with the parts nothing happened to.</td></tr><tr><td><code>wheel</code></td><td><code>&#39;zoom&#39; | &#39;pan&#39;</code></td><td><code>&#39;zoom&#39;</code></td><td>What the wheel does over a pane.</td></tr><tr><td><code>loupe</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the pixels under the pointer are shown magnified.</td></tr><tr><td><code>fade</code></td><td><code>number</code></td><td><code>0.5</code></td><td>How much of the second picture is let through. Overlay only.</td></tr><tr><td><code>onFadeChange</code></td><td><code>(fade: number) =&gt; void</code></td><td>—</td><td>The overlay was faded.</td></tr><tr><td><code>wipe</code></td><td><code>number</code></td><td><code>0.5</code></td><td>Where the line between the two is, from 0 to 1. Wipe only.</td></tr><tr><td><code>onWipeChange</code></td><td><code>(wipe: number) =&gt; void</code></td><td>—</td><td>The line was moved.</td></tr><tr><td><code>marks</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the pixels that changed are tinted.</td></tr><tr><td><code>outlines</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether a box is drawn round each change.</td></tr><tr><td><code>header</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether each side is named above it.</td></tr><tr><td><code>navigation</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the buttons for stepping through the changes are drawn.</td></tr><tr><td><code>zoom</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the zoom controls are drawn.</td></tr><tr><td><code>summary</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the bar under the panes is drawn.</td></tr><tr><td><code>colorScheme</code></td><td><code>&#39;system&#39; | &#39;light&#39; | &#39;dark&#39;</code></td><td><code>&#39;system&#39;</code></td><td>Which palette to draw in.</td></tr><tr><td><code>locale</code></td><td><code>&#39;en&#39; | &#39;ko&#39;</code></td><td><code>&#39;en&#39;</code></td><td>The language of the component&#39;s own words.</td></tr><tr><td><code>strings</code></td><td><a href="./../types/diffine-strings"><code>Partial&lt;DiffineImageStrings&gt;</code></a></td><td>—</td><td>Words to use instead of the locale&#39;s.</td></tr></tbody></table><p><code>split</code> draws two panes; the other three draw one, with both names over it. What the marks are drawn in is five <a href="./../theme#colours">custom properties</a> rather than props, because a canvas is painted rather than styled.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>view</code></td><td><code>DiffineImageView</code></td><td><code>DiffineImageView.split</code></td><td>How the two are laid out.</td></tr><tr><td><code>unchanged</code></td><td><code>DiffineImageUnchanged</code></td><td><code>.keep</code></td><td>What is done with the parts nothing happened to.</td></tr><tr><td><code>wheel</code></td><td><code>DiffineImageWheel</code></td><td><code>.zoom</code></td><td>What the wheel does over a pane.</td></tr><tr><td><code>loupe</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the pixels under the pointer are shown magnified.</td></tr><tr><td><code>fade</code></td><td><code>double?</code></td><td><code>0.5</code></td><td>How much of the second picture is let through. Overlay only.</td></tr><tr><td><code>onFadeChanged</code></td><td><code>ValueChanged&lt;double&gt;?</code></td><td>—</td><td>The overlay was faded.</td></tr><tr><td><code>wipe</code></td><td><code>double?</code></td><td><code>0.5</code></td><td>Where the line between the two is, from 0 to 1. Wipe only.</td></tr><tr><td><code>onWipeChanged</code></td><td><code>ValueChanged&lt;double&gt;?</code></td><td>—</td><td>The line was moved.</td></tr><tr><td><code>marks</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the pixels that changed are tinted.</td></tr><tr><td><code>outlines</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether a box is drawn round each change.</td></tr><tr><td><code>header</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether each side is named above it.</td></tr><tr><td><code>navigation</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the buttons for stepping through the changes are drawn.</td></tr><tr><td><code>zoom</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the zoom controls are drawn.</td></tr><tr><td><code>summary</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the bar under the panes is drawn.</td></tr><tr><td><code>colorScheme</code></td><td><code>DiffineColorScheme</code></td><td><code>.system</code></td><td>Which palette to draw in.</td></tr><tr><td><code>theme</code></td><td><a href="./../theme"><code>DiffineTheme?</code></a></td><td>—</td><td>The whole palette, and the measurements with it.</td></tr><tr><td><code>height</code></td><td><code>double?</code></td><td>—</td><td>How tall the whole comparison is.</td></tr><tr><td><code>locale</code></td><td><code>DiffineLocale</code></td><td><code>DiffineLocale.en</code></td><td>The language of the widget&#39;s own words.</td></tr><tr><td><code>strings</code></td><td><a href="./../types/diffine-strings"><code>DiffineStrings?</code></a></td><td>—</td><td>Words to use instead of the locale&#39;s.</td></tr></tbody></table><p><code>split</code> draws two panes; the other three draw one, with both names over it. What the marks are drawn in is <code>theme.image</code>, seven colours of the <a href="./../theme#the-palette">palette</a>.</p></div><h2 id="moving-around" tabindex="-1">Moving around <a class="header-anchor" href="#moving-around" aria-label="Permalink to &quot;Moving around&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>viewport</code></td><td><code>DiffineImageViewport | &#39;fit&#39;</code></td><td>—</td><td>Where a reader is looking.</td></tr><tr><td><code>defaultViewport</code></td><td><code>DiffineImageViewport | &#39;fit&#39;</code></td><td><code>&#39;fit&#39;</code></td><td>Where to start looking.</td></tr><tr><td><code>onViewportChange</code></td><td><code>(viewport: DiffineImageViewport) =&gt; void</code></td><td>—</td><td>A reader moved or zoomed, or a button did.</td></tr><tr><td><code>selected</code></td><td><code>number</code></td><td>—</td><td>Which change a reader has stepped to, or -1.</td></tr><tr><td><code>defaultSelected</code></td><td><code>number</code></td><td><code>-1</code></td><td>Which change to start on.</td></tr><tr><td><code>onSelectedChange</code></td><td><code>(selected: number, region: DiffImageRegion | null) =&gt; void</code></td><td>—</td><td>A change was stepped to.</td></tr></tbody></table><p><code>DiffineImageViewport</code> is <code>{ scale, x, y }</code>: how many screen pixels one pixel of the frame is drawn as, and the point of the frame the middle of the pane is looking at. Both panes are given the same one, which is what makes a split view move together.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>viewport</code></td><td><code>DiffineImageViewport?</code></td><td>—</td><td>Where a reader is looking. <code>null</code> fits the frame to the pane.</td></tr><tr><td><code>onViewportChanged</code></td><td><code>ValueChanged&lt;DiffineImageViewport&gt;?</code></td><td>—</td><td>A reader moved or zoomed, or a button did.</td></tr><tr><td><code>selected</code></td><td><code>int?</code></td><td>—</td><td>Which change a reader has stepped to, or -1.</td></tr><tr><td><code>defaultSelected</code></td><td><code>int</code></td><td><code>-1</code></td><td>Which change to start on.</td></tr><tr><td><code>onSelectedChanged</code></td><td><code>void Function(int, DiffImageRegion?)?</code></td><td>—</td><td>A change was stepped to.</td></tr></tbody></table><p><code>DiffineImageViewport</code> is <code>{ scale, x, y }</code>: how many logical pixels one pixel of the frame is drawn as, and the point of the frame the middle of the pane is looking at. Both panes are given the same one, which is what makes a split view move together. There is no <code>defaultViewport</code>: <code>null</code> is the fit, and it is also where an uncontrolled pane starts.</p></div>',12)])])}const g=e(a,[["render",r]]);export{p as __pageData,g as default};
