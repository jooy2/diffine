import{_ as t,o as a,c as o,a9 as n}from"./chunks/framework.Bxc0mSCw.js";const w=JSON.parse('{"title":"변경 기록","description":"The first stable release. Each view is its own entry now, so a page carries the comparison it asked for and nothing of the other one. The documents gained…","frontmatter":{"title":"변경 기록","order":1,"editLink":false},"headers":[],"relativePath":"ko/changelog.md","filePath":"ko/changelog.md","lastUpdated":null}'),i={name:"ko/changelog.md"};function s(r,e,h,d,c,l){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h1><blockquote><p>This package&#39;s history. Each language Diffine ships for keeps its own changelog beside its own manifest, because they version independently.</p></blockquote><div class="diffine-fw" data-fw="react"><h2 id="vnext-2026" tabindex="-1">vNext (2026--) <a class="header-anchor" href="#vnext-2026" aria-label="Permalink to &quot;vNext (2026--)&quot;">​</a></h2><h2 id="v1-0-0-2026-09-12" tabindex="-1">v1.0.0 (2026-09-12) <a class="header-anchor" href="#v1-0-0-2026-09-12" aria-label="Permalink to &quot;v1.0.0 (2026-09-12)&quot;">​</a></h2><p>The first stable release. Each view is its own entry now, so a page carries the comparison it asked for and nothing of the other one. The documents gained folding, patches read and written, marks over the whitespace and arrows for taking a change across; the pictures gained a loupe, a mask that can be written out as a file of its own, and a comparison of several at once rather than a pair.</p><h3 id="breaking-changes" tabindex="-1">Breaking changes <a class="header-anchor" href="#breaking-changes" aria-label="Permalink to &quot;Breaking changes&quot;">​</a></h3><ul><li><p><strong>Each view is its own import.</strong> <code>import { TextDiff } from &#39;diffine-react&#39;</code> becomes <code>import { TextDiff } from &#39;diffine-react/text-diff&#39;</code>, and <code>ImageDiff</code> the same way from <code>diffine-react/image-diff</code>. <code>DIFFINE_LANGUAGES</code> moved with the component whose menu it builds, to <code>diffine-react/text-diff</code>. Everything else the root exported is still there: the comparison, the patch reader and writer, and every type.</p><p>What it buys is that the root is now functions and types and nothing else. A page that compares two documents to count what changed carries 2.6 kB and no React, where the same import used to reach a viewer, a stylesheet&#39;s worth of components and a menu of thirty-four languages, and got away with it only where a bundler was clever enough to throw all of that away again. Under one that emits a file per <code>import()</code> before it decides what the build contains — esbuild does — importing <code>diffText</code> used to write 164 kB of syntax grammars into the build. It now writes none.</p></li><li><p><strong><code>DiffineStrings</code> is three types.</strong> <code>DiffineCommonStrings</code> is what both views say, <code>DiffineTextStrings</code> and <code>DiffineImageStrings</code> each extend it with their own, and <code>DiffineStrings</code> is still the two together for an application keeping one table for both. <code>TextDiff</code> takes <code>Partial&lt;DiffineTextStrings&gt;</code> and <code>ImageDiff</code> takes <code>Partial&lt;DiffineImageStrings&gt;</code>, so a partial table written against the old name still fits; a variable annotated <code>DiffineStrings</code> still fits as well. What changed underneath is that the words are shipped the way they are used — a page with a picture comparison on it no longer carries the word for a regular expression.</p></li><li><p><strong><code>languageLabel</code> is off unless a page asks for it.</strong> It was on, which meant every comparison drew the name of a language at the right end of its bar and every editor drew a menu of thirty-four of them — on pages whose documents were not code, and on pages that already knew what their documents were. Pass <code>languageLabel</code> to get it back. Both the name and the menu are now fetched when it is turned on rather than imported, so a page that leaves it off carries neither.</p></li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>virtualize</code> now cuts a wrapped document as well.</strong> Where the rows are was arithmetic and could only be arithmetic, so <code>wrap</code> turned the whole thing off and a wrapped comparison of twenty thousand lines drew twenty thousand rows. Now the rows that have been drawn are measured and kept, the rest stand at the average of those, and the pane is scrolled by however much the row under its top edge moved when a measurement replaced a guess — so the words a reader is looking at stay where they were looking at them. Two panes held level share one table of heights, because two columns of different heights are two scrollbars that cannot both be right.</p><p>It stays off in two places, and both are cases where a row standing in for one that is not drawn would stand in the wrong place: under <code>renderWidget</code>, where what an application draws can grow at any moment, and in a wrapped editor, where the lines are drawn behind a field holding the whole document and the two have to break in the same places.</p></li><li><p><strong>What a reader copies out of a pane is the document, not the page.</strong> The blanks that hold the two sides level are real empty lines in the page and are in neither file, so a document copied through them arrived with a gap wherever the other side was longer. The text is now built from the lines the pane is drawing: blanks and folded bands contribute nothing, the first and last lines are cut where the selection cuts them, and a selection that reaches outside the lines is left to the browser.</p></li><li><p><strong><code>paintDiffImage</code> turns the mask into a picture of its own.</strong> What changed, on a ground that is see-through, the size of the frame — which is the step between a comparison nobody can look at and a file attached to a build. It takes four bytes a colour rather than a CSS string, and writing the file stays the application&#39;s, exactly as reading one is. Together with <code>formatPatch</code>, a comparison of either kind can now leave the page in the form the rest of a toolchain reads.</p></li><li><p><strong><code>diff.ignore</code> takes patterns whose matches do not count.</strong> A snapshot with a timestamp in it, a log with a request id, a build with a hash in its filename: one line that is different every time, and a comparison that says the whole file changed. Each pattern is looked for in both lines and what it finds is set aside, so two lines that differ only inside a match are the same line — and what is set aside is still drawn, exactly as with <code>whitespace</code>.</p><p>A match is set aside rather than removed, so a line with a timestamp and a line with the timestamp missing are still two different lines. It decides which lines are equal, at the level of a line: inside a pair that was edited the words are compared as they were written, because a pattern written for a line is not a pattern about one word of it.</p></li><li><p><strong><code>result.format</code> says how each document is written, and the bar under the panes says so when the two differ.</strong> Every line ending ends a line, so a file written on one platform and edited on another is not a file where every line changed — and the cost of that was a file whose only difference was invisible reading as no difference at all. <code>format</code> carries the line ending each document uses, whether its last line has one, and whether it begins with a byte order mark. It is worked out beside the comparison rather than inside it, so nothing about which lines are equal has changed, and it is left out of a comparison read back out of a patch, which never saw either file.</p></li><li><p><strong><code>showInvisibles</code> draws the whitespace inside the lines.</strong> A dot in the middle of each column a space takes, and a rule under a run of tabs. The characters themselves are untouched and the marks are drawn on the elements around them, so what a reader copies out is the line as it was written. <code>--diffine-invisible</code> is the colour, and <code>strings.format</code>, <code>strings.mixedEndings</code> and <code>strings.noFinalNewline</code> are the words the bar uses.</p></li><li><p><strong><code>applyChanges</code> puts a pair of arrows on every change, for taking it across.</strong> A comparison of a saved version and a draft is read with one question in mind, and this is the answer to it: the arrow pointing left writes the right-hand version over the left, the one pointing right does the opposite, and a <code>readOnly</code> side is never written into — so the usual arrangement leaves one arrow rather than two. The write goes in through the browser&#39;s own editing command, so Ctrl+Z takes it back and <code>onBeforeChange</code> or <code>onAfterChange</code> reports it exactly as a keystroke would.</p><p>It is the editor&#39;s, because applying a change means writing a document, and the buttons sit in the column between the panes, so <code>connectors={false}</code> takes them away with it. <code>strings.applyChange</code> is what they are named with.</p></li><li><p><strong><code>renderGutter</code> and <code>renderWidget</code> are where the application draws its own.</strong> A comparison knows what changed and nothing else, and a review is made of everything else: a comment, a thread, a coverage bar, a lint warning, a button for adding one. The first adds a column to the gutter beside each line and the second puts a box under one. Both are called with the line and the side it is on, for the lines a pane draws rather than for the whole document, and both return <code>null</code> for a line that gets nothing.</p><p>A widget is as tall as it is and can grow at any moment for reasons nothing here would hear about, so <code>virtualize</code> turns itself off while <code>renderWidget</code> is given and a split view gives the line opposite the same height to keep the two sides level. Both props belong to <code>viewer</code> mode: an editor lays a field over its lines, and a column of unknown width or a box of unknown height between them would put the caret in the wrong place. <code>.diffine-slot</code> and <code>.diffine-widget</code> are what they are styled through.</p></li><li><p><strong><code>collapse</code> folds away the runs of unchanged lines nobody is reading.</strong> Two versions of a file are mostly the part nobody edited, and a reader who opened a comparison to see what changed scrolls past all of it. On, each run is drawn as a band saying how many lines it stands for, with <code>context</code> lines kept either side of every change — three by default, which is what <code>diff</code> and <code>git</code> write. Pressing a band puts its lines back. Both panes fold the same runs so a split view stays level, and a band is exactly one line tall, so <code>virtualize</code> carries on unchanged behind it.</p><p>A search reaches the whole document rather than the part of it that is drawn, so opening one puts the folded runs back until the bar is closed. A band is also drawn where a comparison is missing lines rather than hiding them — between one hunk of a patch and the next — whatever <code>collapse</code> says, and that one cannot be pressed, because nobody sent the lines it stands for. <code>strings.folded</code> and <code>strings.expand</code> are the two words it is written with.</p></li><li><p><strong><code>parsePatch</code> reads a unified diff, and <code>formatPatch</code> writes one.</strong> A service that already holds the comparison — a server, a build, a hook with <code>git diff</code> in its hands — can now send the patch instead of both documents, and what comes back is the same value <code>diffText</code> returns, down to the words marked inside a pair of changed lines. <code>parsePatch</code> gives one entry per file the patch covers, each with the names off the <code>---</code> and <code>+++</code> lines; <code>formatPatch</code> takes a comparison and a <code>context</code> and writes the format <code>git apply</code> and <code>patch</code> read.</p><p>What the format does not carry, the reader does not invent. The lines between one hunk and the next are not in a patch, so the numbers jump there: a line&#39;s <code>index</code> is still its own number in the file it came from, and <code>result.before</code> holds the lines that arrived rather than the whole document. Both functions are also their own entry, <code>diffine-react/patch</code>, so nothing of the component reaches a bundle that only wanted them.</p></li><li><p><strong>A pane no longer hides what is inside it from a screen reader.</strong> It was <code>role=&quot;img&quot;</code>, which is the role for what the canvas holds and the wrong one for the pane around it: a role of <code>img</code> makes everything inside it presentational, so the button an empty pane invites a picture with was in the markup and in no accessibility tree. The pane is a <code>group</code> with a name now, which is what it always was. The Flutter package never had this — a <code>Semantics</code> node with <code>image</code> on it does not take its children out of the tree.</p></li><li><p><strong>The magnified pixels stay where they are put, and a reader can put them anywhere.</strong> The panel moved itself to whichever corner the pointer was furthest from, which meant it jumped as a reader read — and a panel that moves to stay out of the way is the thing a reader watches. It now starts against the edge away from the pane being read, has a handle in its bar for dragging it anywhere in the comparison, and a corner for pulling it larger: the tiles stay the size they are and there are more of them, up to forty-one across. It also belongs to the comparison rather than to a pane now, which is what lets it sit over the other one. <code>strings.loupeMove</code> and <code>strings.loupeSize</code> name the two.</p></li><li><p><strong><code>loupe</code> shows the pixels under the pointer, and what colour they are.</strong> Two panes at four hundred per cent say two pixels are different and stop there, and what a reader asks next — the same grey a shade darker, or a different colour altogether — is a number that no amount of zoom gives back. Moving the pointer over either pane now draws the pixels around it magnified, both sides at once, with the colour of the middle one written out as the hex a reader would type back and the point of the frame it sits at. It pins itself to whichever corner the pointer is furthest from, and follows a pointer rather than a finger. On by default; <code>loupe={false}</code> turns it off. <code>strings.at</code> is the word before the coordinates.</p></li><li><p><strong>The wheel zooms about the pointer.</strong> It used to need Ctrl or Cmd held, and a plain wheel either moved a picture larger than its pane or left the page to scroll. That is the right answer for a comparison sitting in the middle of an article and the wrong one for a comparison that is the page, which is most of them — so <code>wheel</code> is now a prop with <code>zoom</code> as its default, Shift to move the picture instead, and <code>wheel=&quot;pan&quot;</code> for the old behaviour.</p><p>A wheel notch also counts in what it says it counts in. <code>deltaMode</code> was ignored, so a mouse on Windows and Firefox, which reports lines rather than pixels, was asking for a zoom of about one part in three hundred — a notch that did nothing at all.</p></li><li><p><strong><code>unchanged</code> decides what happens to the parts of a picture nothing happened to.</strong> <code>marks</code> and <code>outlines</code> say what is done with the pixels that changed, and until now there was no answer for the rest of the frame. <code>dim</code> draws it faint and draws what changed as it is, so the change is what the eye lands on and the rest of the picture is still there to say where in it the change was — which is what the <code>mask</code> view cannot say. <code>hide</code> draws only what changed, on a plain ground, for reading a change as a picture rather than as a mark on one. It holds across all four views, because it is a different question from how the two are laid out, and <code>marks={false}</code> is the pairing for both.</p></li><li><p><strong><code>stats.distance</code> says how far apart two pictures are, not just how much of them moved.</strong> The average distance between two pixels both pictures cover, on the scale <code>tolerance</code> is measured on. Every pixel is in it, including the ones the tolerance and the smoothing test let through, so a picture re-encoded badly and a picture with a panel painted over are no longer the same answer. It costs one addition on the pixels that were being measured anyway.</p></li><li><p><strong><code>ImageDiff</code> takes a list of pictures, not only a pair.</strong> <code>pictures</code> is several rather than two, and passing it is what turns the list on. Each gets a pane of its own with what <em>that</em> picture disagrees with the baseline about marked on it; the pane holding the baseline is marked with everywhere anything disagrees, because the baseline disagrees with nothing and a pane with nothing on it reads as a pane nobody looked at. <code>baseline</code> names which one, <code>picturesResult</code> takes a comparison already worked out, and <code>onPicturesDiff</code> reports one.</p><p>Everything else works as it did: one zoom and one position across every pane, the boxes and the buttons that step through them, the loupe showing every picture at the point under the pointer, and the bar above and below cut into as many parts as there are panes — with the controls in a row of their own past two, since a title with five names in it has no room for buttons. <code>overlay</code> and <code>wipe</code> are a question about two pictures and fall back to <code>split</code> for more than two.</p></li><li><p><strong><code>diffImages</code> compares several pictures at once, and <code>imagesSimilarity</code> says how alike they are.</strong> Three renderings of one screen, four exports of one asset, a saved version against the last five runs: what is wanted there is one frame with every disagreement on it, and a pair has no room for a third. Each picture is compared with a baseline exactly as <code>diffImage</code> would compare it, so every option means what it means there and a list of two is the same answer in a different shape.</p><p>What the list adds is the mask. It is a bit a picture rather than a kind, so <code>mask[pixel] !== 0</code> is &quot;does anything disagree here&quot; and <code>mask[pixel] &amp; (1 &lt;&lt; i)</code> is &quot;does this one&quot; — which is what lets a view tint each picture with what is wrong with that picture. A byte holds eight of them, and <code>MOST_PICTURES</code> is that number. <code>paintDiffImages</code> turns the mask into a picture, of all of them at once or of one on its own.</p><p><code>imagesSimilarity</code> is the shorter question again: one share for the set, and <code>each</code> saying how alike every picture is to the baseline, which is what names the odd one out where the one number only says there is one.</p></li><li><p><strong><code>imageSimilarity</code> answers how alike two pictures are, in one number.</strong> <code>diffImage</code> says where two pictures differ, which is the question a reader looking at them has; a build with a threshold in it, a report ranking a hundred screenshots and a badge on a page are all asking the shorter one. What comes back is a share from 0 to 1, the counts it came from, how large each picture was, and how far apart the pixels are on average — because a photograph saved again is unalike in most of its pixels and barely apart in any of them, and one number cannot say both. It is the whole comparison underneath, so every option means what it means there.</p></li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong>The search bar is fetched when a reader opens one.</strong> It is not on the screen when a comparison is, and for most readers it never is, so it is no longer in what a page downloads to draw two documents. The button that opens it is, because that one is drawn from the start. What it costs is the render between the keystroke and the bar, which is a chunk already sitting beside the page.</p></li><li><p><strong>The syntax highlighter is fetched rather than imported.</strong> highlight.js and the thirty-four grammars were already behind an <code>import()</code> apiece, but the module that reaches them was not, so every page with a viewer on it carried the machinery for colouring whether or not it coloured anything. It is now fetched the first time a viewer is given a <code>language</code>, which is also the first time any of it is any use. A viewer that is given none never asks, and a build that never mentions one never writes the chunk.</p></li><li><p><strong>A <code>highlight</code> of the application&#39;s own no longer fetches a grammar.</strong> The prop replaces <code>language</code>&#39;s colours rather than joining them, so a viewer given both was downloading a grammar whose runs it then threw away. <code>language</code> still names what the bar writes and what the menu starts on.</p></li><li><p><strong>The comparison walks the pixels each picture covers rather than every pixel of the frame.</strong> The inner loop asked, for each pixel, whether it was inside the first picture and inside the second — six pieces of arithmetic to answer a question the rectangles already answer once a row. Each row is now three runs: the middle both pictures cover, and whatever is left at either end. For two pictures of the same size laid corner to corner, which is the usual pair, two of the three are empty. Four million identical pixels compare in 20ms where they took 29, and a photograph against an edited copy of itself in 48ms where it took 56.</p></li><li><p><strong>A picture comparison no longer repaints on every render.</strong> <code>viewport</code> defaults to <code>fit</code>, and a fitted viewport was worked out afresh on each render — a new object each time, which is what the panes watch to decide whether to paint. So both canvases were painted again for every render of the page around them, whatever it was about. The fitted viewport is now held, and the Flutter pane compares what it is about to paint with what it painted instead of repainting on every build.</p></li><li><p><strong><code>npm run size</code> measures what each entry costs, and CI fails a change that outgrows it.</strong> Tree shaking is a property of the build rather than of the source and it is lost quietly — one plain import where an <code>import()</code> used to be, and a page that colours nothing is carrying a syntax highlighter again. The check bundles each entry the way an application would, counts what a page fetches before it draws anything, and compares it against a budget. What is behind an <code>import()</code> is not in that number.</p></li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p><strong>A box round a change is one thin dashed marquee rather than a border with a border round it.</strong> It was a wider line in a contrasting colour with the line itself on top, which is visible on any picture and reads as two borders. The two colours are now drawn at the same width, one solid and one dashed over it, so whichever of the two the photograph matches the other one shows through the gaps. The change a reader has stepped to is solid rather than dashed, which tells it from the rest without making it heavier.</p></li><li><p><strong>A box round a change can be seen on any picture.</strong> The outline was one thin line at 40% of a dark navy, which is invisible over the dark half of a photograph — and the dark half is where the changes tend to be, because that is where a comparison is hardest to read without one. Each box is now drawn twice, a wider line in the new <code>--diffine-image-halo</code> and the line itself over it, so the pair shows whichever of the two colours the picture underneath happens to match. <code>--diffine-image-outline</code> went from 40% to 85% now that it has something behind it.</p></li><li><p><strong>The pixels neither picture reaches are no longer counted as pixels that agree.</strong> Two pictures one of which is wider and the other taller — or two held apart by <code>align</code> — leave a corner of the frame neither of them covers, and <code>stats.unchanged</code> counted it as unchanged while <code>stats.ratio</code> divided by the whole frame. So a 4 × 2 picture against a 2 × 4 one read as two thirds the same when half of what the two of them cover is not. <code>stats.covered</code> is the new denominator, <code>unchanged</code> is now the pixels both cover and agree about, and the four counts add up to <code>covered</code>. Two pictures of the same size are unaffected.</p></li><li><p><strong>A picture comparison redraws when <code>unchanged</code> changes.</strong> The list of things the pane watches to decide whether to paint was missing <code>unchanged</code> and the stencil it builds, so the two new modes only arrived on the next paint something else asked for. <code>react-hooks/exhaustive-deps</code> now knows <code>useIsomorphicLayoutEffect</code> is an effect, which is what should have caught it: the rule had never looked inside any of this package&#39;s layout effects, because the name is the package&#39;s own.</p></li><li><p><strong><code>ignoreAntialiasing</code> no longer excuses an edge that arrived.</strong> It took the stronger of the two pictures&#39; steps, so an edge only one of them has — the boundary of a patch pasted over a flat part of a photograph — was allowed to excuse its own arrival. Half of the boundary column of the cloned patch in the guide came back as pixels that had not changed, at any tolerance. Both pictures now have to have an edge at the pixel and the allowance is the weaker of the two steps, which is what tells an edge drawn twice from an edge that was not there before. Every one of the 61,409 pixels the patch changes is now reported, where 60,429 were, and a page of text nudged by a third of a pixel still has 94% of its differing pixels dropped.</p></li><li><p><strong><code>ignoreAntialiasing</code> no longer swallows a change inside a texture.</strong> It allowed a differing pixel through whenever the pixel lay between the pixels around it and the change was no larger than the range of brightness across them. Nearly every pixel of a photograph lies between its neighbours and the range across a texture is most of the scale, so the allowance was most of the scale too: a patch cloned over the rainy window of a photograph came back as four fifths of the pixels it actually covers, and a change inside fine detail could vanish entirely.</p><p>A pixel now has to have something level within a pixel of it — two pixels of exactly one colour side by side — before the blend it sits in is treated as the edge of anything. A page of text drawn twice is still quiet, because a page is level; a hedge, a knitted blanket and a rainy window are not level anywhere, so nothing in them is dropped. On the photograph the guide compares, 98% of the cloned patch is now reported where 87% was, and a page of text nudged by a third of a pixel still reports 3% of the pixels that differ.</p></li><li><p><strong><code>diffImage</code> says so when a buffer is shorter than the size it was given.</strong> It read the buffer at <code>(y * width + x) * 4</code> and never asked whether the buffer reached that far, and a typed array hands back <code>undefined</code> past its end rather than throwing — so two reads off the end compared equal and a picture one row short came back as two pictures that agree about the row that is missing. Both sides are now measured once before anything reads them, and a buffer that is too small is a <code>RangeError</code> naming the side, the size and the two byte counts. A buffer with room to spare is still fine.</p></li></ul><h2 id="v0-1-0-2026-09-06" tabindex="-1">v0.1.0 (2026-09-06) <a class="header-anchor" href="#v0-1-0-2026-09-06" aria-label="Permalink to &quot;v0.1.0 (2026-09-06)&quot;">​</a></h2><p>The viewer became a component that is also an editor, the text gained colour and a search of its own, and two pictures can now be compared where before only two documents could.</p><h3 id="breaking-changes-1" tabindex="-1">Breaking changes <a class="header-anchor" href="#breaking-changes-1" aria-label="Permalink to &quot;Breaking changes&quot;">​</a></h3><ul><li><strong><code>DiffineViewer</code> is now <code>TextDiff</code>.</strong> Rename the import and the element; every prop it already took means the same thing, and <code>DiffineViewerProps</code> is <code>TextDiffProps</code>. The name says what the component compares rather than how it is read, because a picture is a different kind of diff rather than a different kind of viewer — and the same component now writes as well as reads, which <code>mode</code> decides.</li></ul><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>mode=&quot;editor&quot;</code> is the same view with the two panes made editable.</strong> The same comparison, the same lines, the same bands across the column between them, and the same buttons for stepping through the changes — with a field over each side, so the comparison is worked out again as somebody types into it. One component rather than two, because reading a comparison and writing one were never two things: <code>mode</code> is what decides which of its parts are on the screen, and every other prop means the same in both. <code>before</code>/<code>after</code> or <code>defaultBefore</code>/<code>defaultAfter</code> decide whether the documents are the application&#39;s or the component&#39;s, <code>onBeforeChange</code> and <code>onAfterChange</code> report either way, and <code>readOnly</code> takes a side for the common arrangement of a saved version on the left and a draft on the right.</p></li><li><p><strong>The field is laid over the lines rather than replacing them.</strong> A <code>&lt;textarea&gt;</code> cannot colour a word inside itself, and nothing that can is also an undo stack, an input method, a selection and a control a screen reader already knows how to read. So the field&#39;s own text is invisible, its caret is not, and everything on the screen is drawn behind it — tints, marked words and <code>highlight</code> included. What the two have to agree on is written into the stylesheet rather than measured, so the alignment holds at any size and does not lag a frame behind a resize.</p><p>The two sides are never held level. A blank line put in to keep them in step would be a line somebody could put the caret in, so each document runs at its own length and the column between the panes says which part of one answers which part of the other.</p></li><li><p><strong><code>indentWithTab</code> types a tab, and is off by default.</strong> A control a keyboard cannot leave is a page a keyboard cannot leave. Turned on there are two ways out, both of them the ones somebody would try: Shift+Tab moves back a control, and Escape hands the next Tab to the browser. The tab itself goes in through the browser&#39;s own editing command, so undo still undoes it. <code>spellCheck</code> hands the fields to the browser&#39;s own dictionary, and is off for the same reason a code editor&#39;s is.</p></li><li><p><strong><code>onDiff</code> hands over the comparison every time it is worked out again</strong>, for everything an application wants to say about the two documents outside the box they are in: a count in a heading, or a button that is only worth pressing while the two differ.</p></li><li><p><strong>Each pane carries a search of its own.</strong> A button in the bar above it opens a bar under it, and Ctrl+F opens the one for the pane the keyboard is in — two panes, two queries, two counts, opened and closed one at a time, because a name being chased through the version on the left is not a name being chased through the version on the right. Matches are marked as the query is typed, the pane moves to the one being read, and Enter and Shift+Enter step through the rest. Three switches inside the box read the query as a case-sensitive one, as whole words only, or as a regular expression.</p><p>It reads the document rather than the page, so it reaches the lines <code>virtualize</code> left undrawn and the browser&#39;s own find cannot. <code>search={false}</code> turns the button and the shortcut off together, and <code>--diffine-search</code> and <code>--diffine-search-current</code> are the two colours it marks with.</p></li><li><p><strong>Ctrl+H opens that bar with a row for replacing on it.</strong> The editor&#39;s alone, and not for a side that is <code>readOnly</code>. Replace writes over the match being read and moves to the one that takes its place, so pressing it again walks down the document, and Replace All writes over every one of them. Both go in through the browser&#39;s own editing command, so Ctrl+Z takes them back — and the caret follows the search, which means closing the bar leaves it on the match that was being read.</p></li><li><p><strong><code>--diffine-selection</code> and <code>strings.placeholder</code> join the theming interface.</strong> The selection colour has to be see-through, because the words under a selection are drawn behind the field; the placeholder is what an empty field says before anybody has typed into it.</p></li><li><p><strong><code>language</code> colours the two documents as whatever they are written in.</strong> A highlight.js identifier, or <code>plain</code> for a document that is not code, with <code>DIFFINE_LANGUAGES</code> as the whole list and the name written at the right end of the bar above the panes. An editor draws that list as a menu instead, because a document somebody pasted is a document nobody knew the language of — <code>defaultLanguage</code> and <code>onLanguageChange</code> are the usual pair for holding the choice, and <code>languageLabel</code> turns the name and the menu off together. The menu is built rather than a <code>&lt;select&gt;</code>, so it is drawn in the component&#39;s own style, and it keeps everything a <code>&lt;select&gt;</code> gives a reader: the arrow keys, Home and End, Enter, Escape, and typing a letter to jump to a language.</p><p>The library and each grammar sit behind an <code>import()</code>, so a component left on <code>plain</code> fetches none of it and one asking for Python fetches Python. The colours are eight custom properties, <code>--diffine-code-keyword</code> and the rest, that every class highlight.js emits is mapped onto.</p><p>The colouring is deferred, so it is never what a keystroke waits on: an editor draws the new text against the colours the last keystroke produced, and the new ones arrive in a pass that yields to the next key. On five thousand lines the tokenising costs 48ms where the comparison costs 0.7ms, which is the whole of the difference.</p><p><code>highlight</code> still takes an application&#39;s own highlighter, and replaces <code>language</code> rather than adding to it.</p></li><li><p><strong>The bar under the panes says what each document weighs and what happened between them.</strong> It sits on the header&#39;s grid, so the left half is under the left pane and the right half under the right one, and each side carries its own character count and its size in bytes. The counts go at the far right as a <code>~</code>, a <code>+</code> and a <code>−</code> against three numbers — the same three marks the gutter puts beside a line — and a screen reader is told the sentence instead. <code>documentSize</code> is the string that sentence is written from.</p></li><li><p><strong><code>font</code> sets the typeface, its size, its line height and its letter spacing from props.</strong> The same four custom properties, for an application that holds them in its own state rather than in its own CSS; anything left out keeps the stylesheet&#39;s value. <code>--diffine-letter-spacing</code> is new, and is the fourth of them.</p></li><li><p><strong><code>diffImage</code> compares two pictures pixel by pixel.</strong> A byte a pixel saying what happened to it, the changes grouped into rectangles, where each picture sits in the frame the two were compared in, and the counts — the same bargain <code>diffText</code> makes, made again for pixels, and worth more here: comparing two photographs is a few million pieces of arithmetic, and <code>diffine-react/image</code> is how that happens in a worker. Both sides are <code>ImageData</code>, or anything shaped like it, and neither has to be the size of the other. Opening a file is not part of it.</p></li><li><p><strong><code>tolerance</code> decides how much of a difference counts.</strong> Two pictures saved by the same encoder twice are not the same file twice, and at nought a comparison of them lights up nearly everywhere. What the number is measured against is the distance between two colours, weighted the way an eye weights them, with a fully transparent pixel reading as the white it would be drawn on.</p></li><li><p><strong><code>ignoreAntialiasing</code> drops the pixels a renderer&#39;s own smoothing left behind.</strong> The same page drawn by two browsers differs along every letter and every curve while showing the same thing. A pixel is left out when it is a blend of what surrounds it rather than a colour of its own, and the change is no larger than the step in brightness it is sitting on — so an edge redrawn a fraction of a pixel over is quiet, and a mark that arrived in the middle of a white field is not.</p></li><li><p><strong><code>align</code> finds the offset between two pictures that are not lined up.</strong> A screenshot taken again a pixel to the left is, to a comparison that starts both at the corner, a picture where everything changed. The search is coarse first — the pictures are halved until they are thumbnails, the offset is found there, and each step back up refines it by a pixel — and <code>alignRadius</code> is how far it goes. What only one of the two covers afterwards comes back as <code>added</code> or <code>removed</code>, which is the answer a line with nothing opposite it gets.</p></li><li><p><strong><code>ImageDiff</code> draws all of that.</strong> The changed pixels tinted over both sides, a box round each change, buttons that step from one to the next, and one zoom and one position shared by both panes — so two pictures are never looking at different parts of themselves. Drag to move, Ctrl with the wheel to zoom about the pointer, and the arrows and <code>+</code> and <code>−</code> for a reader who is not holding one. Above its own size the picture is drawn crisp rather than smooth, because a reader at four hundred per cent is counting pixels.</p></li><li><p><strong>Four views rather than one.</strong> <code>split</code> puts one picture either side, <code>overlay</code> fades between them, <code>wipe</code> draws the line where one stops and the other starts, and <code>mask</code> drops both and leaves what changed. No single one of them answers &quot;did this move, or did it change colour&quot;: a wipe does, an overlay does not, and the mask says where to point the other three.</p></li><li><p><strong><code>mode=&quot;editor&quot;</code> takes a picture from the reader.</strong> An empty pane invites one, a full one takes one dropped on it, and each side has a button in the bar for somebody who is not dragging anything. <code>before</code>/<code>after</code> or <code>defaultBefore</code>/<code>defaultAfter</code> decide whether the pictures are the application&#39;s or the component&#39;s, exactly as they do for text.</p></li><li><p><strong>A picture is decoded at four million pixels at most, and <code>maxPixels</code> moves that.</strong> Two photographs out of a modern camera, held as bitmaps and as buffers, are most of a gigabyte before anything has been compared. Past the cap a picture is decoded smaller, which costs a little sharpness at a high zoom and is the difference between a page that answers and a page that stops.</p></li><li><p><strong>Seven custom properties for what a picture comparison paints.</strong> <code>--diffine-image-changed</code>, <code>--diffine-image-added</code> and <code>--diffine-image-removed</code> are the marks, <code>--diffine-image-outline</code> and <code>--diffine-image-marker</code> are the box round a change and the one being looked at, and <code>--diffine-image-ground</code> and <code>--diffine-image-chequer</code> are the pane behind them. They are read off the element and painted into the pixels, because a canvas cannot be styled — which is why the mask is painted again when the palette under it changes.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to &quot;Changed&quot;">​</a></h3><ul><li><p><strong><code>highlight.js</code> is the package&#39;s one dependency</strong>, where there were none. It is never loaded until a <code>language</code> other than <code>plain</code> is asked for, so a page that does not colour its documents downloads nothing it did not before. <code>react</code> and <code>react-dom</code> remain peer dependencies.</p></li><li><p><strong>A band between the panes runs from the colour it left as to the colour it arrived as.</strong> An edit went out on one side and came in on the other, and the band for one used to be a delete-coloured fill with an insert-coloured outline — two statements that contradicted each other. It is one gradient across the column now. The band is drawn as a fill with its two curves stroked separately, so the curves that carry the meaning are not thinned by the two vertical edges that carry none, and the column takes the surface colour rather than the gutter&#39;s so that a pale tint on it reads the way the same tint does inside a pane.</p></li><li><p><strong>The change a reader is on is marked with a heavier line rather than in the accent colour</strong>, so which change it is stays a matter of weight and the colours are left to say what happened.</p></li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to &quot;Fixed&quot;">​</a></h3><ul><li><p>The bar above the panes is given as many columns as the view under it. With <code>connectors={false}</code> the two halves of the header were still laid out around a column that was not there, so each label sat over the wrong document.</p></li><li><p>The line-number gutter runs to the bottom of a pane the document does not fill. It was drawn by each line, so it stopped where the document did, and it now stays still while a long line scrolls past it.</p></li><li><p>A page&#39;s own paragraph margin no longer opens a gap under the panes. The summary bar and the empty message were <code>&lt;p&gt;</code> elements, and a documentation theme&#39;s <code>.vp-doc p</code> beat the reset that takes their margin off.</p></li><li><p>Two panes that are not held level follow each other on the scale each one has. A pane with eighty pixels of travel answers in one part in eighty, which read back against seventy thousand pixels on the other side turned one pixel of rounding into nine hundred — so a change button or a search that scrolled the long side to a line was answered with a jump of half a screen.</p></li><li><p>Switching between the split and unified views no longer warns in the console. <code>useVirtualRows</code> spread the panes&#39; line counts into its dependency lists, and a split view has two panes where a unified view has one.</p></li><li><p>A view handed <code>result</code> on its own draws the comparison rather than the empty message. Emptiness was read off the two document props, which such a view leaves unset.</p></li></ul><h2 id="v0-0-1-2026-09-05" tabindex="-1">v0.0.1 (2026-09-05) <a class="header-anchor" href="#v0-0-1-2026-09-05" aria-label="Permalink to &quot;v0.0.1 (2026-09-05)&quot;">​</a></h2><p>The first published version.</p><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>diffText</code> compares two documents and hands back what it found.</strong> The lines each was split into, the rows a side-by-side view draws, the changes in the order they appear, and the counts. It is a plain object with no React and no DOM in it, so the same call answers a badge on a page, a line in a terminal, and the viewer.</p></li><li><p><strong>The words inside a changed line are compared as well as the lines.</strong> A row that says a line changed is half an answer; <code>inline</code> decides whether the other half is words, graphemes, or nothing. A pair of lines that turns out to have too little in common is left unmarked rather than striped with the punctuation the two happen to share — <code>inlineThreshold</code> is where that line is drawn.</p></li><li><p><strong><code>whitespace</code> and <code>ignoreCase</code> decide which lines count as the same.</strong> Trailing whitespace, whitespace at either end, how much of it there is between two words, or all of it. Whatever is ignored is still drawn, so these change which lines are called equal and never what a reader sees.</p></li><li><p><strong><code>diffWords</code>, <code>diffCharacters</code> and <code>diffSequence</code> are the engine on its own.</strong> The first two compare a heading or a cell without a document around it. The third takes any two arrays of tokens, for an application whose pieces are neither lines nor words.</p></li><li><p><strong><code>maxCost</code> stops two documents with nothing in common from taking the page down with them.</strong> Finding the smallest set of edits costs the size of the documents multiplied by the number of edits between them, and the answer for that case is &quot;all of it changed&quot; — which is not worth waiting for. Past the limit the range comes back as one replacement and <code>complete</code> says the engine gave up.</p></li><li><p><strong><code>diffine-react/diff</code> is an entry of its own</strong>, for an application that wants the comparison without the component.</p></li><li><p><strong><code>DiffineViewer</code> draws two documents side by side.</strong> The matching lines are held level with each other, a line with no counterpart gets a blank opposite it, and the column between the panes draws each change as a band from where it left to where it arrived. Scrolling one pane scrolls the other, and a line that wraps takes its counterpart&#39;s row down with it so the two documents never fall out of step.</p></li><li><p><strong>Every part of the view is a prop with a default.</strong> Line numbers, the <code>+</code> and <code>−</code> markers, wrapping, holding the two sides level, the connectors, the synchronised scrolling, the header, the summary, the tab width, the palette and the language — each one on its own, so the component goes from a full side-by-side down to a bare column of lines without a stylesheet being touched.</p></li><li><p><strong><code>view=&quot;unified&quot;</code> puts what went out above what came in</strong>, in one column, with both documents&#39; line numbers down the side. It is the same comparison read a second way rather than a second comparison.</p></li><li><p><strong>The viewer takes a comparison instead of two documents.</strong> <code>result</code> is for an application that worked one out in a worker, on a server, or once for a list of viewers.</p></li><li><p><strong>Every colour and measurement is a custom property.</strong> An application with a palette of its own overrides <code>--diffine-*</code> on the element rather than writing rules that have to beat the package&#39;s.</p></li><li><p><strong>The viewer says what it is showing to a reader who cannot see it.</strong> Each pane is a named region, every changed line carries the word for what happened to it where a screen reader will find it and a copy will not, and the <code>+</code>, <code>−</code> and <code>~</code> markers say in shape what the colours say in colour.</p></li><li><p><strong>English and Korean, and any other language an application writes itself.</strong> <code>locale</code> picks one of the two and <code>strings</code> replaces any word in either.</p></li><li><p><strong>Two buttons for reading a comparison one change at a time.</strong> They sit in the bar above the panes, with the count beside them, and they wrap — a reader working down a file wants the next change rather than a button that stops at the bottom. The change they land on is marked down its left edge and its band between the panes is drawn in the accent colour, so where a reader is stays visible after the scrolling has stopped.</p><p><code>selected</code> and <code>onSelectedChange</code> make that an application&#39;s to hold, in the usual React pair, and setting <code>selected</code> scrolls the view the same way pressing a button does. <code>navigation</code> turns the buttons off; the bar they sit in is drawn for them even when <code>header</code> is not.</p></li><li><p><strong>A long comparison draws the lines a reader can see and no more.</strong> Twenty thousand lines is twenty thousand rows in the page and forty of them are on the screen; the rest are now height and nothing else. The scrollbar is still the length of the document, the sideways scroll is still the width of its longest line, and the bands between the panes are worked out by arithmetic rather than from elements that are not there.</p><p>It needs every line to be the same height, which only a pane that is not wrapping gives, so <code>wrap</code> turns it off. It also leaves a short document alone. <code>virtualize={false}</code> turns it off outright, which is what a page needs if the browser&#39;s own find has to reach text that is scrolled away.</p></li><li><p><strong><code>highlight</code> lets an application colour the text inside a line</strong>, which is where a syntax highlighter goes. It is handed the whole line — a grammar applied to a fragment does not come out right, and fragments are what a comparison produces — and hands back the runs it wants drawn differently. The line is then cut at the boundaries of both, so a changed word that is half a string literal is drawn as exactly that. With the rows virtualised it is called for the lines on the screen rather than for the document.</p></li></ul></div><div class="diffine-fw" data-fw="flutter"><h2 id="vnext-2026-1" tabindex="-1">vNext (2026--) <a class="header-anchor" href="#vnext-2026-1" aria-label="Permalink to &quot;vNext (2026--)&quot;">​</a></h2><h2 id="v1-0-0-2026-09-12-1" tabindex="-1">v1.0.0 (2026-09-12) <a class="header-anchor" href="#v1-0-0-2026-09-12-1" aria-label="Permalink to &quot;v1.0.0 (2026-09-12)&quot;">​</a></h2><p>The first release. Diffine for Flutter is the React package&#39;s engine and view, written again in Dart — the same comparison, the same rows, the same palette down to the colour values, and the same answers for the same arguments.</p><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to &quot;Added&quot;">​</a></h3><ul><li><p><strong><code>TextDiff</code> compares two documents and draws what happened between them.</strong> Lines are matched first and then the words inside a pair that was edited, so a changed word is a changed word rather than a changed line. Every part of the view is an argument with a default — <code>view</code>, <code>lineNumbers</code>, <code>markers</code>, <code>wrap</code>, <code>alignLines</code>, <code>collapse</code>, <code>context</code>, <code>connectors</code>, <code>syncScroll</code>, <code>header</code>, <code>navigation</code>, <code>search</code>, <code>summary</code>, <code>showInvisibles</code> — so the same widget covers a full side-by-side with connectors and a bare column of lines in a panel too narrow for anything else.</p><p>Only the lines a reader can see are built, whichever way those arguments are set: the panes are lists rather than columns, so a comparison of twenty thousand lines draws the forty on the screen and the scrollbar is still the length of the document. There is no <code>virtualize</code> to turn off, because there was nothing left to turn off.</p></li><li><p><strong><code>mode: DiffineMode.editor</code> is the same view with the two panes made editable.</strong> The field&#39;s own text is see-through and its caret is not, and the lines behind it are painted — so a tinted row, a marked word and a highlighter&#39;s colours sit under text somebody is typing, while the field goes on being a field with the framework&#39;s own undo, input method, selection and accessibility. <code>before</code>/<code>after</code> or <code>defaultBefore</code>/<code>defaultAfter</code> decide whether the documents are the application&#39;s or the widget&#39;s, <code>onBeforeChanged</code> and <code>onAfterChanged</code> report either way, and <code>readOnly</code> takes a side.</p><p><code>applyChanges</code> puts a pair of arrows on every change in the column between the panes, for the question a comparison of a saved version and a draft is usually read with: keep this, or put the other one back.</p></li><li><p><strong><code>ImageDiff</code> compares two pictures and draws what changed over both of them.</strong> Four views rather than one — side by side, faded over each other, wiped across, or the mask on its own — because no single one of them answers &quot;did this move, or did it change colour&quot;. <code>tolerance</code> sets how much of a difference counts, <code>ignoreAntialiasing</code> drops what a renderer&#39;s smoothing left behind, and <code>align</code> finds the offset between two shots that are not lined up.</p><p><code>loupe</code> shows the pixels under the pointer magnified, both sides at once, with the colour of the middle one written out and the point of the frame it sits at — because two panes at four hundred per cent say two pixels are different and stop there.</p><p>The wheel zooms about the pointer, which <code>wheel</code> turns back into moving the picture for a comparison sitting in the middle of a screen that scrolls.</p><p><code>pictures</code> is several rather than two, and passing it is what turns the list on: each gets a pane of its own with what that picture disagrees with the baseline about marked on it, and the pane holding the baseline is marked with everywhere anything disagrees.</p><p><code>unchanged</code> decides what happens to the parts nothing happened to: drawn faint with the change at full strength, or not drawn at all so that the change is read as a picture on a plain ground rather than as a mark on one.</p><p>A picture arrives as the bytes of a file, as a <code>ui.Image</code>, or as a buffer of pixels. There is no URL among them: fetching one is the application&#39;s to do. <code>onChoose</code> is the same refusal for opening a file — the widget draws the button and the application answers it, because a picker is a plugin and which plugin is the application&#39;s choice.</p></li><li><p><strong><code>diffImages</code> compares several pictures at once, and <code>imagesSimilarity</code> says how alike they are.</strong> Three renderings of one screen, four exports of one asset, a saved version against the last five runs. Each picture is compared with a baseline exactly as <code>diffImage</code> would compare it, and the mask is a bit a picture rather than a kind — so <code>mask[pixel] != 0</code> is &quot;does anything disagree here&quot; and <code>mask[pixel] &amp; (1 &lt;&lt; i)</code> is &quot;does this one&quot;. A byte holds eight, which is <code>kMostPictures</code>.</p></li><li><p><strong><code>imageSimilarity</code> answers how alike two pictures are, in one number.</strong> <code>diffImage</code> says where two pictures differ, which is the question a reader looking at them has; a build with a threshold in it, a report ranking a hundred screenshots and a badge on a screen are all asking the shorter one. What comes back is a share from 0 to 1, the counts it came from, how large each picture was, and how far apart the pixels are on average — because a photograph saved again is unalike in most of its pixels and barely apart in any of them, and one number cannot say both.</p></li><li><p><strong>The engine is its own thing.</strong> <code>diffText</code>, <code>diffWords</code>, <code>diffCharacters</code>, <code>diffSequence</code>, <code>diffImage</code>, <code>imageSimilarity</code>, <code>paintDiffImage</code>, <code>parsePatch</code> and <code>formatPatch</code> touch no widget at all, so a build script, an isolate or a test can call them and hand the answer to the widget as a value. <code>diffSequence</code> takes any two lists of tokens, for an application whose pieces are neither lines nor words.</p></li><li><p><strong><code>parsePatch</code> reads a unified diff into exactly what <code>diffText</code> returns</strong>, down to the words marked inside a pair of changed lines, and <code>formatPatch</code> writes one back out. A service that already holds the comparison can send a few kilobytes of patch instead of a few megabytes of documents. Where a patch is missing lines the numbers jump, and the viewer draws a band saying how many rather than pretending line 7 sits above line 40.</p></li><li><p><strong>A search of its own for each pane</strong>, with a button in the bar above it, a bar underneath it, and Ctrl+F for whichever pane the keyboard is in. Case, whole words and regular expressions are three switches inside the box; in the editor Ctrl+H opens the same bar with a row for replacing under it.</p></li><li><p><strong><code>language</code> colours the two documents</strong>, from grammars that ship with the package. The identifiers are highlight.js&#39;s, so a <code>language</code> that works in a browser works here, and <code>kDiffineLanguages</code> is the whole list. The grammars are approximate on purpose — an app bundle has no network to fetch a real parser from, and colour is not the kind of answer that has to be right — and they never change the document: every run is cut out of the text it was given and the lengths add back up to the line. <code>highlight</code> is the way in for an application that has a highlighter of its own.</p></li><li><p><strong><code>renderGutter</code> and <code>renderWidget</code> are where the application draws its own</strong>: a review comment, a coverage bar, a lint warning, a button for adding one. A widget is as tall as it is, and the line opposite is given the same height so the two sides stay level.</p></li><li><p><strong><code>DiffineTheme</code> is the palette as a value.</strong> The React package declares every colour as a custom property and an application overrides the property; there is no cascade here, so the same names arrive as an object instead and <code>copyWith</code> replaces what an application means to change. It travels with the widget rather than through a global, so one comparison can be dark inside a light screen.</p></li><li><p><strong>English and Korean</strong>, through <code>locale</code>, with <code>strings</code> for an application whose words are its own or whose language is neither. Everything the colours say is said in words as well: which side a pane is, what happened to each line, how many changes there are, and what every control does.</p></li><li><p><strong>No Material and no Cupertino.</strong> Nothing in the package imports either, so a comparison sits inside a <code>MaterialApp</code>, a <code>CupertinoApp</code> or a bare <code>WidgetsApp</code> without bringing a second design system with it. The only dependency is <code>characters</code>, which is what Flutter already ships.</p></li></ul></div>',4)])])}const g=t(i,[["render",s]]);export{w as __pageData,g as default};
