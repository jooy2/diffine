import{_ as n,C as c,o as r,c as s,a9 as i,j as d,E as a,a as t}from"./chunks/framework.Bxc0mSCw.js";const m=JSON.parse('{"title":"TextDiff","description":"Two documents side by side or in one column, read or typed into. Every option, its type and its default.","frontmatter":{"title":"TextDiff","order":1,"description":"Two documents side by side or in one column, read or typed into. Every option, its type and its default."},"headers":[{"level":2,"title":"Modes","slug":"modes","link":"#modes","children":[]},{"level":2,"title":"The documents","slug":"the-documents","link":"#the-documents","children":[]},{"level":2,"title":"The view","slug":"the-view","link":"#the-view","children":[]},{"level":2,"title":"Typing","slug":"typing","link":"#typing","children":[]},{"level":2,"title":"Which change a reader is on","slug":"which-change-a-reader-is-on","link":"#which-change-a-reader-is-on","children":[]},{"level":2,"title":"Searching a pane","slug":"searching-a-pane","link":"#searching-a-pane","children":[]}],"relativePath":"api/components/text-diff.md","filePath":"en/api/components/text-diff.md","lastUpdated":1789182987000}'),h={name:"api/components/text-diff.md"};function l(f,e,g,p,u,b){const o=c("Fw");return r(),s("div",null,[e[14]||(e[14]=i('<h1 id="textdiff" tabindex="-1"><code>TextDiff</code> <a class="header-anchor" href="#textdiff" aria-label="Permalink to &quot;`TextDiff`&quot;">​</a></h1><div class="diffine-fw" data-fw="react"><div class="language-tsx vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">tsx</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TextDiff</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> before</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{saved} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">after</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{draft} /&gt;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TextDiff</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;editor&quot;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defaultBefore</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{saved} </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defaultAfter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{draft} /&gt;</span></span></code></pre></div></div><div class="diffine-fw" data-fw="flutter"><div class="language-dart vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dart</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TextDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(before</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> saved, after</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> draft);</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">TextDiff</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> DiffineMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.editor, defaultBefore</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> saved, defaultAfter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> draft);</span></span></code></pre></div></div><h2 id="modes" tabindex="-1">Modes <a class="header-anchor" href="#modes" aria-label="Permalink to &quot;Modes&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>mode</code></td><td><code>&#39;viewer&#39; | &#39;editor&#39;</code></td><td><code>&#39;viewer&#39;</code></td><td>Whether the two documents are read or written.</td></tr></tbody></table></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>mode</code></td><td><code>DiffineMode</code></td><td><code>DiffineMode.viewer</code></td><td>Whether the two documents are read or written.</td></tr></tbody></table></div>',6)),d("p",null,[a(o,{react:"One component draws both.",flutter:"One widget draws both."}),e[0]||(e[0]=t()),e[1]||(e[1]=d("code",null,"editor",-1)),e[2]||(e[2]=t(" lays a field over each pane, so the comparison is worked out again as somebody types into it; everything else — the rows, the tints, the marked words, the bands, the buttons, the search — is the same in both.",-1))]),e[15]||(e[15]=i('<div class="diffine-fw" data-fw="react"><p>Several props are ignored in <code>editor</code> mode: <code>view</code>, <code>alignLines</code>, <code>collapse</code>, <code>context</code>, <code>renderGutter</code> and <code>renderWidget</code>, because a field cannot be a unified column, cannot be padded out with blanks somebody could type into, cannot hide the lines somebody is typing, and cannot have something of the application&#39;s own between its lines; and <code>result</code>, because a comparison worked out elsewhere is a comparison of documents nobody has typed into yet. Going the other way, <code>readOnly</code>, <code>indentWithTab</code>, <code>spellCheck</code>, <code>defaultBefore</code>, <code>defaultAfter</code> and <code>onLanguageChange</code> do nothing in <code>viewer</code> mode.</p></div><div class="diffine-fw" data-fw="flutter"><p>Several arguments are ignored in <code>DiffineMode.editor</code>: <code>view</code>, <code>alignLines</code>, <code>collapse</code>, <code>context</code>, <code>renderGutter</code> and <code>renderWidget</code>, because a field cannot be a unified column, cannot be padded out with blanks somebody could put the caret in, cannot hide the lines somebody is typing, and cannot have something of the application&#39;s own between its lines; and <code>result</code>, because a comparison worked out elsewhere is a comparison of documents nobody has typed into yet. Going the other way, <code>readOnly</code>, <code>indentWithTab</code>, <code>defaultBefore</code>, <code>defaultAfter</code> and <code>onLanguageChanged</code> do nothing in <code>DiffineMode.viewer</code>.</p></div><h2 id="the-documents" tabindex="-1">The documents <a class="header-anchor" href="#the-documents" aria-label="Permalink to &quot;The documents&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>before</code></td><td><code>string | DiffineSource</code></td><td><code>&#39;&#39;</code></td><td>The document on the left.</td></tr><tr><td><code>after</code></td><td><code>string | DiffineSource</code></td><td><code>&#39;&#39;</code></td><td>The document on the right.</td></tr><tr><td><code>defaultBefore</code></td><td><code>string | DiffineSource</code></td><td><code>&#39;&#39;</code></td><td>What the left field starts with. Editor only.</td></tr><tr><td><code>defaultAfter</code></td><td><code>string | DiffineSource</code></td><td><code>&#39;&#39;</code></td><td>What the right field starts with. Editor only.</td></tr><tr><td><code>onBeforeChange</code></td><td><code>(value: string) =&gt; void</code></td><td>—</td><td>The left document was typed into.</td></tr><tr><td><code>onAfterChange</code></td><td><code>(value: string) =&gt; void</code></td><td>—</td><td>The right document was typed into.</td></tr><tr><td><code>onDiff</code></td><td><code>(result: DiffResult) =&gt; void</code></td><td>—</td><td>The comparison, every time it is worked out again.</td></tr><tr><td><code>readOnly</code></td><td><code>boolean | &#39;before&#39; | &#39;after&#39;</code></td><td><code>false</code></td><td>Which side cannot be typed into. Editor only.</td></tr><tr><td><code>result</code></td><td><a href="./../types/diff-result"><code>DiffResult</code></a></td><td>—</td><td>A comparison already worked out. <code>before</code> and <code>after</code> are ignored. Viewer only.</td></tr><tr><td><code>diff</code></td><td><a href="./../types/diff-options"><code>DiffOptions</code></a></td><td>—</td><td>How the two are compared.</td></tr></tbody></table><p><code>DiffineSource</code> is <code>{ content: string; label?: string }</code>. The label is what the header calls that side; without one it is the word for it in the current locale.</p><p>A document nobody can type into is read from the props on every render. An editable one is the usual React pair instead: passing <code>before</code> or <code>after</code> makes that document the application&#39;s, passing <code>defaultBefore</code> or <code>defaultAfter</code> leaves it to the component, and which of the two it is, is decided on the first render. <code>onBeforeChange</code> and <code>onAfterChange</code> are called whichever of the two is holding it.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>before</code></td><td><code>String?</code></td><td>—</td><td>The document on the left.</td></tr><tr><td><code>after</code></td><td><code>String?</code></td><td>—</td><td>The document on the right.</td></tr><tr><td><code>beforeLabel</code></td><td><code>String?</code></td><td>—</td><td>What the header calls the left side.</td></tr><tr><td><code>afterLabel</code></td><td><code>String?</code></td><td>—</td><td>What it calls the right side.</td></tr><tr><td><code>defaultBefore</code></td><td><code>String?</code></td><td>—</td><td>What the left field starts with. Editor only.</td></tr><tr><td><code>defaultAfter</code></td><td><code>String?</code></td><td>—</td><td>What the right field starts with. Editor only.</td></tr><tr><td><code>onBeforeChanged</code></td><td><code>ValueChanged&lt;String&gt;?</code></td><td>—</td><td>The left document was typed into.</td></tr><tr><td><code>onAfterChanged</code></td><td><code>ValueChanged&lt;String&gt;?</code></td><td>—</td><td>The right document was typed into.</td></tr><tr><td><code>onDiff</code></td><td><code>ValueChanged&lt;DiffResult&gt;?</code></td><td>—</td><td>The comparison, every time it is worked out again.</td></tr><tr><td><code>readOnly</code></td><td><code>DiffineSide?</code></td><td>—</td><td>Which side cannot be typed into. Editor only.</td></tr><tr><td><code>result</code></td><td><a href="./../types/diff-result"><code>DiffResult?</code></a></td><td>—</td><td>A comparison already worked out. <code>before</code> and <code>after</code> are ignored. Viewer only.</td></tr><tr><td><code>diff</code></td><td><a href="./../types/diff-options"><code>DiffOptions</code></a></td><td><code>kDiffineDefaults</code></td><td>How the two are compared.</td></tr></tbody></table><p>The document and its name are two arguments rather than one value, because a <code>String</code> is already the whole document. Without a label the header writes the word for that side in the current locale.</p><p>A document nobody can type into is read from the widget on every build. An editable one is controlled or uncontrolled: passing <code>before</code> or <code>after</code> makes that document the application&#39;s, passing <code>defaultBefore</code> or <code>defaultAfter</code> leaves it to the widget, and which of the two it is, is decided on the first build. <code>onBeforeChanged</code> and <code>onAfterChanged</code> are called whichever of the two is holding it.</p></div><h2 id="the-view" tabindex="-1">The view <a class="header-anchor" href="#the-view" aria-label="Permalink to &quot;The view&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>view</code></td><td><code>&#39;split&#39; | &#39;unified&#39;</code></td><td><code>&#39;split&#39;</code></td><td>One document either side, or one column with both.</td></tr><tr><td><code>lineNumbers</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether each line carries its number.</td></tr><tr><td><code>markers</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether a changed line carries a <code>+</code>, <code>−</code> or <code>~</code>.</td></tr><tr><td><code>wrap</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether a long line wraps or runs off the side.</td></tr><tr><td><code>alignLines</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether a line is held level with its counterpart.</td></tr><tr><td><code>collapse</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether runs of unchanged lines far from a change are folded away.</td></tr><tr><td><code>context</code></td><td><code>number</code></td><td><code>3</code></td><td>How many unchanged lines are kept either side of a change.</td></tr><tr><td><code>connectors</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether each change is drawn as a band between the panes.</td></tr><tr><td><code>applyChanges</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether each change carries buttons for writing it into the other document.</td></tr><tr><td><code>syncScroll</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether scrolling one pane scrolls the other.</td></tr><tr><td><code>header</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether each side is named above it.</td></tr><tr><td><code>navigation</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the buttons for moving between changes are drawn.</td></tr><tr><td><code>search</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether a reader can search a pane from inside the component.</td></tr><tr><td><code>summary</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether the bar under the view is drawn.</td></tr><tr><td><code>virtualize</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Whether only the lines a reader can see are drawn.</td></tr><tr><td><code>showInvisibles</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the spaces and tabs inside a line are drawn.</td></tr><tr><td><code>language</code></td><td><code>string</code></td><td><code>&#39;plain&#39;</code></td><td>What the documents are written in, so they are coloured as it.</td></tr><tr><td><code>defaultLanguage</code></td><td><code>string</code></td><td><code>&#39;plain&#39;</code></td><td>Which one to start on, when the component is to keep it.</td></tr><tr><td><code>onLanguageChange</code></td><td><code>(language: string) =&gt; void</code></td><td>—</td><td>A language was chosen from the menu. Editor only.</td></tr><tr><td><code>languageLabel</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether that language is drawn at the right end of the bar.</td></tr><tr><td><code>tabSize</code></td><td><code>number</code></td><td><code>4</code></td><td>How wide a tab is drawn, in characters.</td></tr><tr><td><code>colorScheme</code></td><td><code>&#39;system&#39; | &#39;light&#39; | &#39;dark&#39;</code></td><td><code>&#39;system&#39;</code></td><td>Which palette to draw in.</td></tr><tr><td><code>font</code></td><td><a href="./../types/diffine-font"><code>DiffineFont</code></a></td><td>—</td><td>The typeface the documents are drawn in.</td></tr><tr><td><code>locale</code></td><td><code>&#39;en&#39; | &#39;ko&#39;</code></td><td><code>&#39;en&#39;</code></td><td>The language of the component&#39;s own words.</td></tr><tr><td><code>strings</code></td><td><a href="./../types/diffine-strings"><code>Partial&lt;DiffineTextStrings&gt;</code></a></td><td>—</td><td>Words to use instead of the locale&#39;s.</td></tr><tr><td><code>highlight</code></td><td><a href="./../types/diffine-highlight"><code>DiffineHighlight</code></a></td><td>—</td><td>An application&#39;s own highlighter, in place of <code>language</code>.</td></tr><tr><td><code>renderGutter</code></td><td><a href="./../types/diffine-render"><code>DiffineRender</code></a></td><td>—</td><td>Something of the application&#39;s own, in the gutter beside each line.</td></tr><tr><td><code>renderWidget</code></td><td><a href="./../types/diffine-render"><code>DiffineRender</code></a></td><td>—</td><td>Something of the application&#39;s own, under each line.</td></tr></tbody></table><p><code>collapse</code> and <code>context</code> are the viewer&#39;s; an editor holds whole documents in its fields and folds nothing. <code>applyChanges</code> is the editor&#39;s, because applying a change means writing a document, and its buttons live in the column <code>connectors</code> draws. <code>connectors</code> and <code>syncScroll</code> are about the space between two panes, so both are ignored in the unified view. <code>languageLabel</code> draws the name of the language in <code>viewer</code> mode and the menu it was chosen from in <code>editor</code> mode; <code>language</code>, <code>defaultLanguage</code> and <code>onLanguageChange</code> are the usual pair for that choice.</p><p>Anything else the component is given goes straight to the element, so <code>id</code>, <code>className</code>, <code>style</code> and the <code>aria-*</code> attributes behave as they would on a <code>&lt;div&gt;</code>.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>view</code></td><td><code>DiffineView</code></td><td><code>DiffineView.split</code></td><td>One document either side, or one column with both.</td></tr><tr><td><code>lineNumbers</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether each line carries its number.</td></tr><tr><td><code>markers</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether a changed line carries a <code>+</code>, <code>−</code> or <code>~</code>.</td></tr><tr><td><code>wrap</code></td><td><code>bool</code></td><td><code>false</code></td><td>Whether a long line wraps or runs off the side.</td></tr><tr><td><code>alignLines</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether a line is held level with its counterpart.</td></tr><tr><td><code>collapse</code></td><td><code>bool</code></td><td><code>false</code></td><td>Whether runs of unchanged lines far from a change are folded away.</td></tr><tr><td><code>context</code></td><td><code>int</code></td><td><code>3</code></td><td>How many unchanged lines are kept either side of a change.</td></tr><tr><td><code>connectors</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether each change is drawn as a band between the panes.</td></tr><tr><td><code>applyChanges</code></td><td><code>bool</code></td><td><code>false</code></td><td>Whether each change carries buttons for writing it into the other document.</td></tr><tr><td><code>syncScroll</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether scrolling one pane scrolls the other.</td></tr><tr><td><code>header</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether each side is named above it.</td></tr><tr><td><code>navigation</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the buttons for moving between changes are drawn.</td></tr><tr><td><code>search</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether a reader can search a pane from inside the widget.</td></tr><tr><td><code>summary</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether the bar under the view is drawn.</td></tr><tr><td><code>showInvisibles</code></td><td><code>bool</code></td><td><code>false</code></td><td>Whether the spaces and tabs inside a line are drawn.</td></tr><tr><td><code>language</code></td><td><code>String?</code></td><td>—</td><td>What the documents are written in, so they are coloured as it.</td></tr><tr><td><code>defaultLanguage</code></td><td><code>String</code></td><td><code>&#39;plain&#39;</code></td><td>Which one to start on, when the widget is to keep it.</td></tr><tr><td><code>onLanguageChanged</code></td><td><code>ValueChanged&lt;String&gt;?</code></td><td>—</td><td>A language was chosen from the menu. Editor only.</td></tr><tr><td><code>languageLabel</code></td><td><code>bool</code></td><td><code>true</code></td><td>Whether that language is drawn at the right end of the bar.</td></tr><tr><td><code>colorScheme</code></td><td><code>DiffineColorScheme</code></td><td><code>.system</code></td><td>Which palette to draw in.</td></tr><tr><td><code>theme</code></td><td><a href="./../theme"><code>DiffineTheme?</code></a></td><td>—</td><td>The whole palette, and the measurements with it.</td></tr><tr><td><code>font</code></td><td><a href="./../types/diffine-font"><code>DiffineFont?</code></a></td><td>—</td><td>The typeface the documents are drawn in.</td></tr><tr><td><code>height</code></td><td><code>double?</code></td><td>—</td><td>How tall the whole comparison is. <code>double.infinity</code> fills what holds it.</td></tr><tr><td><code>locale</code></td><td><code>DiffineLocale</code></td><td><code>DiffineLocale.en</code></td><td>The language of the widget&#39;s own words.</td></tr><tr><td><code>strings</code></td><td><a href="./../types/diffine-strings"><code>DiffineStrings?</code></a></td><td>—</td><td>Words to use instead of the locale&#39;s.</td></tr><tr><td><code>highlight</code></td><td><a href="./../types/diffine-highlight"><code>DiffineHighlight?</code></a></td><td>—</td><td>An application&#39;s own highlighter, in place of <code>language</code>.</td></tr><tr><td><code>renderGutter</code></td><td><a href="./../types/diffine-render"><code>DiffineRender?</code></a></td><td>—</td><td>Something of the application&#39;s own, in the gutter beside each line.</td></tr><tr><td><code>renderWidget</code></td><td><a href="./../types/diffine-render"><code>DiffineRender?</code></a></td><td>—</td><td>Something of the application&#39;s own, under each line.</td></tr></tbody></table><p><code>collapse</code> and <code>context</code> are the viewer&#39;s; an editor holds whole documents in its fields and folds nothing. <code>applyChanges</code> is the editor&#39;s, because applying a change means writing a document, and its buttons live in the column <code>connectors</code> draws. <code>connectors</code> and <code>syncScroll</code> are about the space between two panes, so both are ignored in the unified view. <code>languageLabel</code> draws the name of the language in the viewer and the menu it was chosen from in the editor; <code>language</code>, <code>defaultLanguage</code> and <code>onLanguageChanged</code> are the usual pair for that choice.</p><p>There is no <code>virtualize</code>: the rows are built as they are reached, always, because that is what a <code>ListView</code> is. <code>renderWidget</code> is what turns it off, and it does so on its own — what an application draws under a line can grow at any moment, and a row standing in for one of those would be standing in the wrong place.</p><p><code>tabSize</code> is on the theme rather than here, because how wide a tab is drawn is a measurement and the measurements live together.</p></div><h2 id="typing" tabindex="-1">Typing <a class="header-anchor" href="#typing" aria-label="Permalink to &quot;Typing&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>indentWithTab</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether Tab types a tab instead of moving to the next control.</td></tr><tr><td><code>spellCheck</code></td><td><code>boolean</code></td><td><code>false</code></td><td>Whether the browser marks its own spelling mistakes.</td></tr></tbody></table><p>Both are <code>editor</code> mode&#39;s. With <code>indentWithTab</code> on, <strong>Shift+Tab</strong> moves back a control and <strong>Escape</strong> hands the next Tab to the browser, so the field is never one a keyboard cannot leave.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it decides</th></tr></thead><tbody><tr><td><code>indentWithTab</code></td><td><code>bool</code></td><td><code>false</code></td><td>Whether Tab types a tab instead of moving to the next control.</td></tr></tbody></table><p>Editor only. With it on, <strong>Shift+Tab</strong> moves back a control and <strong>Escape</strong> hands the next Tab to the framework, so the field is never one a keyboard cannot leave.</p><p>There is no <code>spellCheck</code>. Spelling is the platform&#39;s on the platforms that have it and nothing at all on the ones that do not, and an argument that did nothing on half of them would be a promise the widget cannot keep.</p></div><h2 id="which-change-a-reader-is-on" tabindex="-1">Which change a reader is on <a class="header-anchor" href="#which-change-a-reader-is-on" aria-label="Permalink to &quot;Which change a reader is on&quot;">​</a></h2><div class="diffine-fw" data-fw="react"><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>selected</code></td><td><code>number</code></td><td>—</td><td>The change being looked at, or -1.</td></tr><tr><td><code>defaultSelected</code></td><td><code>number</code></td><td><code>-1</code></td><td>The one to start on.</td></tr><tr><td><code>onSelectedChange</code></td><td><code>(selected: number, change: DiffChange | null) =&gt; void</code></td><td>—</td><td>A change was moved to.</td></tr></tbody></table><p><code>selected</code> is an index into <code>changes</code>. Passing it makes it the application&#39;s, in the usual React pair, and setting it scrolls the view exactly as pressing a button does. <code>onSelectedChange</code> is called whichever of the two is holding it.</p></div><div class="diffine-fw" data-fw="flutter"><table tabindex="0"><thead><tr><th>Argument</th><th>Type</th><th>Default</th><th>What it is</th></tr></thead><tbody><tr><td><code>selected</code></td><td><code>int?</code></td><td>—</td><td>The change being looked at, or -1.</td></tr><tr><td><code>defaultSelected</code></td><td><code>int</code></td><td><code>-1</code></td><td>The one to start on.</td></tr><tr><td><code>onSelectedChanged</code></td><td><code>void Function(int, DiffChange?)?</code></td><td>—</td><td>A change was moved to.</td></tr></tbody></table><p><code>selected</code> is an index into <code>changes</code>. Passing it makes it the application&#39;s, and setting it scrolls the view exactly as pressing a button does. <code>onSelectedChanged</code> is called whichever of the two is holding it.</p></div><h2 id="searching-a-pane" tabindex="-1">Searching a pane <a class="header-anchor" href="#searching-a-pane" aria-label="Permalink to &quot;Searching a pane&quot;">​</a></h2><p>Each pane is searched on its own: a button in the bar above it opens a bar of its own underneath it, and <strong>Ctrl+F</strong> — <strong>Cmd+F</strong> where that is the modifier — opens the one for the pane the keyboard is in. The two sides have two queries, two counts and two bars, and neither closes the other.</p><p>Matches are marked as the query is typed, the pane moves to the one being read, and <strong>Enter</strong> and <strong>Shift+Enter</strong> step through the rest. The three switches inside the box read the query as a case-sensitive one, as whole words only, and as a regular expression. <strong>Escape</strong> closes the bar.</p>',17)),d("p",null,[e[3]||(e[3]=t("The editor adds a row for replacing, which ",-1)),e[4]||(e[4]=d("strong",null,"Ctrl+H",-1)),e[5]||(e[5]=t(" opens together with the bar. The replacement is written as the text it is — ",-1)),e[6]||(e[6]=d("code",null,"$1",-1)),e[7]||(e[7]=t(" is a dollar and a one — and it goes in through ",-1)),a(o,{react:"the browser's own editing command, so Ctrl+Z takes it back",flutter:"the field's own controller, so the platform's undo takes it back"}),e[8]||(e[8]=t(". A ",-1)),e[9]||(e[9]=d("code",null,"readOnly",-1)),e[10]||(e[10]=t(" side is searched and not replaced in.",-1))]),d("p",null,[e[11]||(e[11]=t("A pane whose search is open still draws only the lines a reader can see, so a match found on line nine thousand is scrolled to and drawn there. ",-1)),a(o,{react:"`search={false}`",flutter:"`search: false`"}),e[12]||(e[12]=t(" turns the button and the shortcuts off together, which is what a ",-1)),a(o,{react:"page",flutter:"screen"}),e[13]||(e[13]=t(" wants if those keys belong to something else on it.",-1))])])}const y=n(h,[["render",l]]);export{m as __pageData,y as default};
